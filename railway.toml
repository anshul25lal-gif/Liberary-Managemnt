[build]
builder = "nixpacks"
buildCommand = "mvn clean package -DskipTests"

[deploy]
startCommand = "java -Xms128m -Xmx512m -jar target/library-management-system-1.0.0.jar"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
